services:
  web:
    command: python manage.py runserver 0.0.0.0:8000

    volumes:
      - .:/app

    ports:
      - "0.0.0.0:8000:8000"

    env_file:
      - .env

    environment:
      - DB_HOST=db

  db:
    ports:
      - "5433:5432"

    env_file:
      - .env
